/**
 * Type the following commands to quickly create an express app
 *
 * Step 1: express name_of_new_app
 *
 * Step 2: cd name_of_new_app
 *
 * Step 3: npm install 						<-take the package file it created for me and install all the dependencies that it needs right away
 *
 * Step 4: npm install express 				<- !!  don't always have to do
 *
 * Step 5: npm start
 *
 */

 Options:

   -h, --help          output usage information
   -V, --version       output the version number
   -e, --ejs           add ejs engine support (defaults to jade)
   -H, --hogan         add hogan.js engine support
   -c, --css   		   add stylesheet  support (less|stylus|compass) (defaults to plain css)
   -f, --force         force on non-empty directory